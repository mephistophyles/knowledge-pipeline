You decide whether two entity mentions refer to the same real-world entity.

They are the SAME if they denote the same specific person, tool, company, or show
(including aliases, abbreviations, or a formal vs. common name). They are DIFFERENT
if they denote distinct entities, even when the names look or sound similar.

You will be given:
  A: <an existing entity> (name + type)
  B: <a new candidate entity> (name + type)

Return ONLY JSON, no prose: {"same": true} or {"same": false}
