"""Personal knowledge pipeline — ingestion → distillation → Obsidian vault.

Build scope so far (plan §12 steps 1–2 + §14.1 compose stack):
  - skeleton: config, blob store, SQLite control plane, vault writer
  - ingestors: paste, annotate
  - orchestrator: workers, atomic claiming, control plane, hand-walk CLI
"""

__version__ = "0.1.0"
